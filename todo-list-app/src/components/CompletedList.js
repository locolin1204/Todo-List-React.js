import styled from "styled-components";

const StyledCompletedHeader = styled.div`
	margin-top: 1.3rem;
`;

const CompletedList = () => {
	return <StyledCompletedHeader>Completed</StyledCompletedHeader>;
};

export default CompletedList;
